<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="viewModel"
            type="com.iwelogic.portfolio.ui.apps.AppsViewModel" />
    </data>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/background">

        <com.iwelogic.portfolio.views.SwipeRefreshLayoutWithoutIndicator
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:onRefreshListener="@{() -> viewModel.onReload()}">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/list"
                apps="@{viewModel.apps}"
                onClick="@{viewModel.onClick}"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                android:orientation="vertical"
                android:overScrollMode="never"
                android:paddingTop="@dimen/default_size"
                app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager" />

        </com.iwelogic.portfolio.views.SwipeRefreshLayoutWithoutIndicator>

        <include
            layout="@layout/layout_progress"
            app:progress="@{viewModel.progress}" />

        <include
            layout="@layout/layout_error"
            app:viewModel="@{viewModel.getBaseViewModel()}" />

    </FrameLayout>

</layout>